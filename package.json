{
  "name": "node-learning",
  "version": "1.0.0",
  "description": "## Usage - In cases where there are so many ouputs, console is cleared to see only the output corresponding the the currently learning topic. comment out `console.log()` to see those.   - To better understand the code, go through the commit history of the file",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js"
  },
  "repository": {
    "type": "git",
    "url": "\\"
  },
  "author": "Aathil",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.14"
  }
}
