//setTimeout executes the handler function once the timeout is expired
setTimeout(() => {
    console.log('Timer is done')
}, 100)
console.log('here')  //this will be executed before the above time handler function.